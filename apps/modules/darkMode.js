export default (page) => {
  switch (page) {
    case 'index':
      const trending = document.getElementById("section__trending");
      console.log(trending)
      break;

    default:
      break;
  }
}